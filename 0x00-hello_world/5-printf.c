#include <stdio.h>

/**
 *main - this function demonstrates the usage of printf
 *
 *Description: the function prints a statement to the console
 *Return: 0 after successful execution of the program
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
